{
  "name": "QuadBrowser",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "package-windows": "electron-packager . QuadBrowser --overwrite --icon=assets/icons/win/icon.ico --platform=win32 --arch=x64 --prune=true --out=dist",
    "package-mac": "electron-packager . QuadBrowser --overwrite --icon=assets/icons/mac/icon.hqx --platform=darwin --arch=x64 --prune=true --out=dist",
    "package-mac-dmg": "electron-installer-dmg dist/QuadBrowser-darwin-x64 QuadBrowser icon=assets/icons/mac/icon.hqx"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^6.0.1",
    "electron-installer-dmg": "^3.0.0",
    "electron-packager": "^14.0.4"
  },
  "dependencies": {
    "electron-context-menu": "^0.14.0",
    "electron-prompt": "^1.3.1",
    "iohook": "^0.6.1"
  },
  "iohook": {
    "targets": [
      "node-64",
      "electron-73"
    ],
    "platforms": [
      "win32",
      "darwin"
    ],
    "arches": [
      "x64"
    ]
  }
}
